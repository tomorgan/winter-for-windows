<Application x:Class="WinterForWindows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WinterForWindows"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <tb:TaskbarIcon x:Key="NotifyIcon"
                        IconSource="icon.ico"
                        ToolTipText="Winter for Windows"
                        MenuActivation="RightClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="🎄 Winter for Windows" IsEnabled="False" FontWeight="Bold"/>
                    <Separator/>
                    <MenuItem Header="Fairy Lights" Name="MenuFairyLights" IsCheckable="True"/>
                    <MenuItem Header="Snow" Name="MenuSnow" IsCheckable="True"/>
                    <MenuItem Header="Penguin" Name="MenuPenguin" IsCheckable="True"/>
                    <MenuItem Header="Christmas Countdown" Name="MenuCountdown" IsCheckable="True"/>
                    <Separator/>
                    <MenuItem Header="Check for Updates..." Name="MenuCheckUpdates"/>
                    <MenuItem Header="Exit" Name="MenuExit"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>
